# Config folder

This folder is divided in different subfolders based on the type of newspaper to generate.

## Config file structure

The config file is .json file and follow this structure:

## Hugging face token

In order to use the banner generator, which is based on the AdImageNet Dataset, you need to set up your hf_token in a file in this folder called
"hf_token.txt". Since it contains sensitive data the file is added to the .gitignore, so you must manually set it in your personal repo.

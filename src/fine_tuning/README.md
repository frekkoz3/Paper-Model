# Fine Tuning

## Hugging Face

In order to push to the hugging face repo the fine tuned models it is required a write-access token.

Once you have it add it in a .env file under the voice "HF_TOKEN=hf_xxxxxxx".
